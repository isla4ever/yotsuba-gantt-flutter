name: yotsuba_gantt_example
description: Yotsuba Gantt Flutter example.
publish_to: none
version: 0.1.0-alpha.1+2

environment:
  sdk: '>=3.4.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  # The demo intentionally consumes the released pub.dev package.
  yotsuba_gantt: 0.1.0-alpha.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
